import React from 'react'
import { Button, Text } from 'react-native'

export default props => {
    function criaBotao(inc, dec) {
        return num
    }

    return (
        <Text style = {{fontSize:35}}> {num} </Text>
    )
}